<% @products.each do |product| %>
<div class="product">
<%= link_to product.name, "/products/#{product.id}" %><br>
<%= product.description %><br>
<%= product.formatted_price %><br>
<%= button_to 'Add to Cart', line_items_path(product_id: product) %>
</div>
<% end %>
